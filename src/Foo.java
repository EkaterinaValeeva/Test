import javax.management.Query;
import java.util.Arrays;


/**This is JavaDoc for class
 * Custom component retrieving data from external SQL DB
 * And here's the end of paragraph
 */
public class Foo {
    public Foo() {
    }

    /**this is the main method that prints different stuff on screen
     * Now describe its parameters
     * <ul>
     *     <li>
     *         {@link Foo1 CorrectLink} @link Foo1 IncorrectLink}
     *     </li>
     *     <ul>
     *         <li>One item to see embedded list</li>
     *
     *     </ul>
     * </ul>
      * @param args a list of arguments to be parsed and printed out on thee screen
     */

    public static void main(String[] args) {
        long t = 1234566;
        Foo1 f = new Foo1();

        f.printData("hello");
        f.printData(5);


        //getData(symbol);
    }

    /**method returning data
     *
     * @param data specifies data to parse
     */
    class SQLite implements Storage {
        /**
         * DBMS-related prefix for connection string.
         */
        private static final String dbmsPrefix = "jdbc:sqlite:";
    }

    public static String getData(String data)
    {
        return data;
        Animals creature = new Animals();


    }
}